[project]
name = "speedia"
version = "0.1.2"
description = "Batch speed test for Clash/Mihomo subscriptions"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "requests>=2.32.0",
]

[project.scripts]
speedia = "speedia:main"

[tool.uv]
package = true
